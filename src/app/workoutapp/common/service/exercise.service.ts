import { Injectable, Signal, linkedSignal, signal, computed } from '@angular/core';
import * as exerciseData from '../static/exercise.json';

@Injectable({
  providedIn: 'root'
})
export class ExerciseService {
  // exerciseOptions: Signal<any[]> = [];
  constructor() { }
}
